#include <stdio.h>	// The stdio.h library contains input and output functions

int main()
{
	int a = 10;

	// Format specifiers are placeholders for variable data
	printf( a displayed as base 10 integer: %1\n", A);	// %i is for integers
	printf( a displayed as base 8 integer: %1\n", A);	// %i is for integers
	printf( a displayed as base 16 integer: %1\n", A);	// %i is for integers
	printf( a displayed as base 16 integer: %1\n", A);	// %i is for integers
	printf( a displayed as base 10 integer: %1\n", A);	// %i is for integers

	return 0;i
}
